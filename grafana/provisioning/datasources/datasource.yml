apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Inside the compose network this resolves to the prometheus service.
    url: http://prometheus:9090
    isDefault: true
    editable: true
